# Workspace instructions

- Do not build, compile, link, or run automated build commands after making changes unless the user explicitly asks for a build or compilation check.
- Prefer source inspection, static checks, and `git diff --check` when verification is appropriate.
